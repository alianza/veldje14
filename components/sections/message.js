import styles from "../../styles/modules/utils.module.scss"

export default function Message() {
    return <section className={styles.section}>
        <h1 className={styles.title}>Hope to see you there!</h1>
    </section>
}
